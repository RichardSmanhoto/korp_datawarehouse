cubes:
  - name: IdentificadorWeb
    sql_table: "`viasoft_elt`.`{COMPILE_CONTEXT.securityContext.schema}.IdentificadorWeb`"
    title: Geral Identificador Web
    
    meta:
      - versao: "__COMMIT_SHA__"

    measures:
      - name: count
        type: count

    dimensions:
      - name: id
        sql: Id
        type: string
        primary_key: true

      - name: idWeb
        sql: IdWeb
        type: string

      - name: entidade
        sql: Entidade
        type: string
        title: Entidade
