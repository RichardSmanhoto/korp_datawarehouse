cubes:
  - name: VendaEquipe
    sql_table: "`viasoft_elt_{COMPILE_CONTEXT.securityContext.schema}`.`{COMPILE_CONTEXT.securityContext.schema}_VendaEquipe`"
    title: Venda Equipe

    meta:
      - versao: "__COMMIT_SHA__"

    measures:
      - name: count
        type: count

    dimensions:
      - name: id
        sql: Id
        type: string
        primary_key: true
        title: Id

      - name: descricao
        sql: Descricao
        type: string
        title: Descricao
