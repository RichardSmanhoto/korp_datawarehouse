cubes:
  - name: StatusRegistro
    sql_table: "`viasoft_elt_{COMPILE_CONTEXT.securityContext.databaseName}`.`{COMPILE_CONTEXT.securityContext.schema}_StatusRegistro`"
    title: Geral Registro Ativo
    
    meta:
      - versao: "__COMMIT_SHA__"

    measures:
      - name: count
        type: count

    dimensions:
      - name: id
        sql: Id
        type: string
        primary_key: true

      - name: ativo
        sql: Ativo
        type: boolean
