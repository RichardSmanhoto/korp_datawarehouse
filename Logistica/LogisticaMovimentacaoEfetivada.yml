cubes:
  - name: LogisticaMovimentacaoEfetivada
    sql_table: "`viasoft_elt_{COMPILE_CONTEXT.securityContext.schema}`.`{COMPILE_CONTEXT.securityContext.schema}_LogisticaMovimentacaoEfetivada`"
    title: Logística Movimentação Efetivada
    meta:
      - versao: "__COMMIT_SHA__"

    measures:
      - name: count
        type: count

    dimensions:
      - name: id
        sql: Id
        type: string
        primary_key: true
        title: Id

      - name: efetiva
        sql: Efetiva
        type: boolean
        title: Efetiva
